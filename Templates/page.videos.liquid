<div class="page-content page-full">

    {%- render 'breadcrumbs' -%}

    <div class="rte rte--nomargins">
        <header class="section-header">
            <h1 class="section-header__title text-center">{{ page.title }}</h1>
        </header>

        <div>
            {{ page.content }}
        </div>

        <div>

            {% section 'youtube-video-and-text' %}

        </div>

    </div>


</div>
